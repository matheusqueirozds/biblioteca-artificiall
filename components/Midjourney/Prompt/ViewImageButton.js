import React from "react";
import { Button } from "./PromptStyles";

const ViewImageButton = ({ onClick }) => {
	return <Button onClick={onClick}>Visualizar imagem</Button>;
};

export default ViewImageButton;
